"""Pytest config at the repo root.

Its mere presence puts the repo root on sys.path, so `import engine` resolves
from anywhere (engine/tests/..., kernels/tests/..., etc.). No fixtures needed yet.
"""
